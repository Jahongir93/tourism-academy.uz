# ── Claude Code (hech qachon yuklanmasin) ──────────────────────
.claude/

# ── Laravel ────────────────────────────────────────────────────
/vendor/
/node_modules/
/.env
/.env.backup
/.env.*.local
/.env.production
.env.example.local

# ── Laravel compiled / cache ───────────────────────────────────
/bootstrap/cache/*.php
/storage/framework/cache/data/*
/storage/framework/sessions/*
/storage/framework/testing/*
/storage/framework/views/*
/storage/logs/*

# ── Uploaded user files ───────────────────────────────────────
# Uploads live in public/storage now. Ignore everything there EXCEPT the
# public CMS content (news/events/homepage/logos) so it ships via git.
# Personal docs (admission/students/employees) stay ignored for privacy.
/storage/app/public/
/public/storage/*
!/public/storage/cms

# ── System & IDE ───────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.suo
*.user

# ── Composer & npm ────────────────────────────────────────────
composer.phar
npm-debug.log
yarn-error.log

# ── Secrets / keys ────────────────────────────────────────────
*.pem
*.key
*.p12
*.pfx
license-server/

# ── Mobile / face system (alohida repo bo'lishi kerak) ────────
mobile-app/
face_attendance_system/

# ── Temp ──────────────────────────────────────────────────────
*.log
*.cache
*.tmp
